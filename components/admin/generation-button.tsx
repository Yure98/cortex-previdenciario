"use client";import{useState}from"react";import{useRouter}from"next/navigation";
export function GenerationButton({caseId,officeId}:{caseId:string;officeId:string}){const r=useRouter();const[s,setS]=useState("idle");async function run(){setS("running");const x=await fetch("/api/gerar",{method:"POST",headers:{"content-type":"application/json","idempotency-key":crypto.randomUUID()},body:JSON.stringify({caso_id:caseId,escritorio_id:officeId,tipo_operacao:"peticao"})});setS(x.ok?"done":"error");if(x.ok)r.refresh();}return <div className="admin-action-stack"><button className="primary-button" type="button" onClick={run} disabled={s==="running"}>{s==="running"?"Motor em execução…":"Rodar / reprocessar motor"}</button>{s==="error"?<small className="error-text">Geração falhou ou foi bloqueada.</small>:null}</div>}
